/*
* AUTO-GENERATED FILE - DO NOT MODIFY
*
* This SQL file was generated by a code generation tool. Any modifications
* made to this file may be overwritten by subsequent code generation
* processes and could lead to inconsistencies or errors in the application.
*
* For any required changes, please modify the source templates or the
* code generation tool's configurations and regenerate this file.
*
* WARNING: MODIFYING THIS FILE DIRECTLY CAN LEAD TO UNEXPECTED BEHAVIOR
* AND IS STRONGLY DISCOURAGED.
*/
drop function if exists deribit.public_auth;

create or replace function deribit.public_auth(
    grant_type deribit.public_auth_request_grant_type,
    client_id text,
    client_secret text,
    refresh_token text,
    timestamp bigint,
    signature text,
    nonce text default null,
    data text default null,
    state text default null,
    scope text default null
)
returns deribit.public_auth_response_result
language sql
as $$
    
    with request as (
        select row(
            grant_type,
            client_id,
            client_secret,
            refresh_token,
            timestamp,
            signature,
            nonce,
            data,
            state,
            scope
        )::deribit.public_auth_request as payload
    )
    , http_response as (
        select deribit.internal_jsonrpc_request(
            '/public/auth'::deribit.endpoint, 
            request.payload, 
            'deribit.non_matching_engine_request_log_call'::name
        ) as http_response
        from request
    )
    select (jsonb_populate_record(
        null::deribit.public_auth_response, 
        convert_from((a.http_response).body, 'utf-8')::jsonb)).result
    from http_response a

$$;

comment on function deribit.public_auth is 'Retrieve an Oauth access token, to be used for authentication of ''private'' requests.';
